<div class="mx-16">
    <dl class="text-center w-full bg-teal-700">
        <dd class="text-2xl font-semibold text-teal-400"><%= @job.name %></dd> 
        <button class="float-right mr-2 bg-teal-400 py-2 px-4 border border-transparent 
                rounded-md text-base font-medium text-white hover:bg-teal-300" type="submit" class="w-100">Apply</button>
        <dd class="text-teal-200">Location: <%= @adresse.street + ", " + @adresse.city + ", " + @adresse.country %></dd> 
        <dd class="text-teal-200">Entreprise: <%= @entreprise.name %></dd> 
        <dd class="text-teal-200">Contract type: todo</dd>
        <% unless @job.dateAdded.nil? %>
            <dd class="text-teal-200">Date added: <%= @job.dateAdded %></dd>
        <% end %>
    </dl>

    <div>
        <h2 class="text-xl font-medium text-teal-400">Job description</h2>
        <p><%= @job.description %></p>
        <h2 class="text-xl font-medium text-teal-400">Job expectations</h2>
        <p><%= @job.expectations %></p>
        <h2 class="text-xl font-medium text-teal-400">Experience required</h2>
        <p><%= @experiences.name %></p>
    </div>
</div>

